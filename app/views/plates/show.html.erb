<% content_for :title, "Plate #{@plate.display_name}" %>
<% content_for :head do %>
  <%= stylesheet_link_tag 'plates_show' %>
  <%= stylesheet_link_tag 'scxrd/gallery' %>
<% end %>

<script type="module">
  import 'scxrd/rod_image_parser';
  import 'scxrd/diffraction_viewer';
  import 'scxrd/card_toggler';
  import 'scxrd/reciprocal_lattice_viewer';
  import 'scxrd/gallery';

  // Set the WASM asset path for the ROD parser
  window.ROD_WASM_PATH = '<%= asset_path("rod_decoder.wasm") %>';
</script>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="mb-0">Plate: <%= @plate.display_name %></h1>
  <%= plate_action_buttons(@plate) %>
</div>

<!-- Collapsible Plate Metadata -->
<div class="card mb-4">
  <div class="card-header p-2">
    <button class="btn btn-link p-0 text-decoration-none d-flex align-items-center w-100" 
            type="button" 
            data-bs-toggle="collapse" 
            data-bs-target="#plateMetadataCollapse" 
            aria-expanded="false" 
            aria-controls="plateMetadataCollapse">
      <i class="fas fa-info-circle me-2 text-muted"></i>
      <span class="text-muted">Plate Information & Storage</span>
      <i class="fas fa-chevron-down ms-auto text-muted"></i>
    </button>
  </div>
  <div class="collapse" id="plateMetadataCollapse">
    <div class="card-body">
      <div class="row">
        <div class="col-md-8">
          <%= render 'plate_details_content' %>
        </div>
        <div class="col-md-4">
          <%= render 'storage_location_content' %>
        </div>
      </div>
    </div>
  </div>
</div>

<%= render 'bulk_upload_form' %>

<%= render 'wells_grid' %>

<%= render 'points_of_interest' %>

<!-- Collapsible Help Card -->
<div class="card mt-4">
  <div class="card-header p-2">
    <button class="btn btn-link p-0 text-decoration-none d-flex align-items-center w-100" 
            type="button" 
            data-bs-toggle="collapse" 
            data-bs-target="#wellsHelpCollapse" 
            aria-expanded="false" 
            aria-controls="wellsHelpCollapse">
      <i class="fas fa-question-circle me-2 text-muted"></i>
      <span class="text-muted">Well Colors & Keyboard Shortcuts</span>
      <i class="fas fa-chevron-down ms-auto text-muted"></i>
    </button>
  </div>
  <div class="collapse" id="wellsHelpCollapse">
    <div class="card-body py-3">
      <div class="row">
        <div class="col-md-6">
          <h6 class="mb-2">
            <i class="fas fa-palette me-2"></i>Well Color Legend
          </h6>
          <div class="mb-3">
            <small class="d-block mb-1">
              <span class="badge me-2" style="background-color: #28a745; color: white;">Green</span> 
              Wells with images or PXRD data
            </small>
            <small class="d-block mb-1">
              <span class="badge me-2" style="background-color: #ffc107; color: black;">Yellow</span> 
              Wells with content only
            </small>
            <small class="d-block">
              <span class="badge me-2" style="background-color: #dc3545; color: white;">Red</span> 
              Empty wells
            </small>
          </div>
        </div>
        <div class="col-md-6">
          <h6 class="mb-2">
            <i class="fas fa-keyboard me-2"></i>Keyboard Shortcuts
          </h6>
          <div class="row">
            <div class="col-6">
              <small class="d-block mb-1">
                <kbd>S</kbd> Toggle multi-select
              </small>
              <small class="d-block">
                <kbd>E</kbd> Edit selected wells
              </small>
            </div>
            <div class="col-6">
              <small class="d-block mb-1">
                <kbd>C</kbd> Clear selection
              </small>
              <small class="d-block">
                <kbd>Esc</kbd> Close/Cancel
              </small>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%= render 'well_modal' %>
<%= render 'edit_multiple_wells_modal' %>

<script type="module">
  import initializePlatesShow from 'plates_show';
  initializePlatesShow();
</script>