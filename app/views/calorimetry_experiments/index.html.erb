<% content_for :title, "Calorimetry Experiments" %>

<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Calorimetry Experiments</h2>
        <%= link_to "Create New Experiment", new_calorimetry_experiment_path, class: "btn btn-primary" %>
      </div>

      <% if @calorimetry_experiments.any? %>
        <div class="table-responsive">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Name</th>
                <th>Plate</th>
                <th>Recorded At</th>
                <th>Video File</th>
                <th>Datasets</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% @calorimetry_experiments.each do |experiment| %>
                <tr>
                  <td>
                    <%= link_to experiment.name, experiment, class: "text-decoration-none" %>
                    <% if experiment.description.present? %>
                      <br><small class="text-muted"><%= truncate(experiment.description, length: 50) %></small>
                    <% end %>
                  </td>
                  <td>
                    <%= link_to experiment.plate.barcode, plate_path(experiment.plate), class: "text-decoration-none" %>
                    <% if experiment.plate.name.present? %>
                      <br><small class="text-muted"><%= experiment.plate.name %></small>
                    <% end %>
                  </td>
                  <td>
                    <%= time_ago_in_words(experiment.recorded_at) %> ago
                    <br><small class="text-muted"><%= experiment.recorded_at.strftime("%B %d, %Y at %l:%M %p") %></small>
                  </td>
                  <td>
                    <% if experiment.video_file.attached? %>
                      <span class="badge bg-success">
                        <i class="fas fa-video"></i>
                        <%= number_to_human_size(experiment.video_file.byte_size) %>
                      </span>
                      <br><small class="text-muted"><%= experiment.video_file.filename %></small>
                    <% else %>
                      <span class="badge bg-secondary">No video</span>
                    <% end %>
                  </td>
                  <td>
                    <% dataset_count = experiment.calorimetry_datasets.count %>
                    <span class="badge bg-<%= dataset_count > 0 ? 'info' : 'secondary' %>">
                      <%= dataset_count %> dataset<%= 's' unless dataset_count == 1 %>
                    </span>
                  </td>
                  <td>
                    <div class="btn-group" role="group">
                      <%= link_to "View", experiment, class: "btn btn-sm btn-outline-primary" %>
                      <%= link_to "Edit", edit_calorimetry_experiment_path(experiment), class: "btn btn-sm btn-outline-secondary" %>
                      <%= button_to "Delete", experiment, 
                                    method: :delete,
                                    class: "btn btn-sm btn-outline-danger",
                                    form: { style: "display: inline;" },
                                    data: { 
                                      confirm: "Are you sure? This will also delete all associated datasets and datapoints."
                                    } %>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="text-center py-5">
          <div class="mb-3">
            <i class="fas fa-flask fa-3x text-muted"></i>
          </div>
          <h4 class="text-muted">No Calorimetry Experiments</h4>
          <p class="text-muted">Create your first calorimetry experiment to get started.</p>
          <%= link_to "Create Experiment", new_calorimetry_experiment_path, class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>