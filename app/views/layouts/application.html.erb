<!DOCTYPE html>
<html>
  <head>
    <title>CrystalPlates</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag 'favicon.ico' %>

    <!-- Rails styles -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Bootstrap CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    
    <!-- Visual-Heatmap for SCXRD diffraction image visualization -->
    <script src="https://unpkg.com/visual-heatmap@2.2.0/dist/visualHeatmap.min.js"></script>
    <script>
      // Debug: Check if Visual Heatmap loaded
      window.addEventListener('load', function() {
        console.log('Visual Heatmap library loaded:', !!window.VisualHeatmap);
        if (window.VisualHeatmap) {
          console.log('VisualHeatmap constructor:', typeof window.VisualHeatmap);
        }
      });
    </script>
    

    
    <!-- Rails JavaScript -->
    <%= javascript_importmap_tags %>
    
    <!-- CifVis for 3D crystal structure visualization (loaded as ES module) -->
    <script type="module" src="<%= asset_path('cifvis.alldeps.js') %>" defer></script>
    
    <!-- Additional head content from views -->
    <%= yield :head %>
  </head>

  <body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
      <div class="container">
        <%= link_to "CrystalPlates", root_path, class: "navbar-brand" %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            
            <li class="nav-item">
              <%= link_to "Chemicals", chemicals_path, class: "nav-link #{'active' if controller_name == 'chemicals'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Stock Solutions", stock_solutions_path, class: "nav-link #{'active' if controller_name == 'stock_solutions'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Locations", locations_path, class: "nav-link #{'active' if controller_name == 'locations'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Plates", plates_path, class: "nav-link #{'active' if controller_name == 'plates'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "PXRD Patterns", pxrd_patterns_path, class: "nav-link #{'active' if controller_name == 'pxrd_patterns'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "SCXRD Datasets", scxrd_datasets_path, class: "nav-link #{'active' if controller_name == 'scxrd_datasets' && params[:well_id].blank?}" %>
            </li>
          </ul>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Settings", settings_path, class: "nav-link #{'active' if controller_name == 'settings'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "API Docs", api_docs_path, class: "nav-link #{'active' if controller_name == 'pages' && action_name == 'api_docs'}" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <!-- Bootstrap JS (CDN) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
